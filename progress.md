Original prompt: 전반적으로는 나쁘지 않은데 약간의 버그와 기능 개선이 되었으면 하는 부분이 있어. 용 차지 공격 크기/양손 크로스 이펙트, 점프 중 차지 모션 버그, 파워업별 이펙트 크기 증가, 게임오버 버튼/최소 프레임 보장을 개선한다.

## 2026-04-25
- Adjusted dragon melee dimensions so charged attacks are smaller at base size and both normal/charged effects grow with power level.
- Changed charged dragon claw visual to two crossed slash sets to read as a two-hand attack.
- Fixed airborne charge-ready animation so the dragon keeps the jump animation in the air instead of switching to charge pose.
- Added GameOver input delay and separate retry/title handlers to prevent click-through retry and allow returning to title.
- `pnpm lint` and `pnpm build` pass.
- Browser Use could not attach because no active Codex browser pane was available; local Playwright validation covered title start gating, hidden dragon selection, normal attack effect, charged jump animation path, charged cross slash visual, GameOver early input guard, and title return.
- Avoid naming scene guard fields `inputEnabled`; Phaser input handling can collide with that name during pointer processing.
- GameOver options now share the same font size/spacing, support delayed keyboard selection with ArrowUp/ArrowDown and Enter/Space, and preserve the click-through guard.
- Cleaned disconnected artifact components from dragon-player frame 20 so the hurt sprite no longer shows neighboring-sprite debris.
- Implemented visual/input pass: procedural natural ground textures with invisible collision, train boss passenger car + expanded hit bounds, lens-origin camera beam hazard, stronger cyan shield aura, manual-gated intro progression, Z/X title start, and a new runtime victory ending background with lower character staging.
- Validation: `pnpm lint` and `pnpm build` pass. Local dev server used at `http://localhost:5174/game`. Playwright state checks confirmed Z/X title start, intro early-input lock, no auto-advance after story, and manual advance to StageScene. Visual screenshots were reviewed under `output/playwright-visual-pass/`, including refined ground, train car, camera beam, shield aura, and victory scene.
- Follow-up feedback: replaced the added passenger-car approach with a single complete 96x64 train locomotive runtime spritesheet (`boss-train-complete`) so the stage 1 boss no longer reads as a cut-off front segment. Relaxed intro gating so it only blocks accidental rapid input for 900ms, then allows skipping before all story lines finish. Revalidated `pnpm lint`, `pnpm build`, and screenshots under `output/playwright-visual-pass/14-train-single-locomotive-stopped.png`.
- Train quality pass: generated two text-prompt-only candidate sheets for a KTX-inspired boss locomotive with no real logos/text, selected the second sheet's top-left candidate, extracted it via chroma key, and built `src/game/assets/sprites/boss-train-v5.png` as a 4-frame 96x64 spritesheet. `BootScene` now loads `boss-train-v5.png`; previous runtime-drawn train placeholder was removed. Candidate and in-game review artifacts are under `output/imagegen/train-boss-v5/`. `pnpm lint` and `pnpm build` pass.
- Detail pass: stage 1 boss fights now pass a `rail` ground surface into `ScrollManager`, rendering a rail-track ground texture only for the train boss arena. Dragon melee boss checks now extend normal boss melee reach by 24px and charged boss melee reach by 32px without changing mob melee range. Verified with `pnpm lint`, `pnpm build`, `output/playwright-visual-pass/15-train-boss-rail-ground.png`, and runtime bounds checks showing normal 66->90 and charged 88->120 against bosses.
- Follow-up detail pass: refined the train boss rail ground from top-down sleepers to a side-view rail bed so the train sits more naturally on the track (`output/playwright-visual-pass/16-train-boss-side-rail.png`). Restored `dragon-player-v1.png` to the pre-clean backup and stopped using sprite-image edits for the dragon; remaining collision tuning is code-only through frame-specific hitboxes focused on the torso/head, excluding attack arm and tail. Runtime check confirms scaled attack frames use 15x30 body bounds. Enlarged/recentered bride shield aura and shell; reviewed at `output/playwright-visual-pass/17-bride-shield-adjusted.png`. `pnpm lint` and `pnpm build` pass.
- Revalidated the no-sprite-edit dragon path after restoration: `dragon-player-v1.png` hash matches `output/imagegen/dragon-player-v1/dragon-player-v1-before-edge-clean.png`; Playwright runtime check confirms dragon frame 0 uses 13.5x30 body bounds and attack frames 18/19 use 15x30 bounds while keeping the original sprite texture. No console/page errors.
- Hitbox debug pass: added hidden title input `kaist11` to enable a persistent hitbox overlay. Red outlines show hurtboxes, yellow outlines show attacks/contact damage/bullets/melee/beam segments, and blue outlines show wall/ground colliders across StageScene and BossScene. Added shared bounds helpers so debug rectangles match Arcade bodies for player/enemy/bullets and existing boss hit bounds for bosses; beam collision now uses the player body instead of visual bounds. Dragon sprite cleanup is limited to alpha-only removal of small detached left/right edge noise components; preview is at `output/imagegen/dragon-player-v1/dragon-edge-noise-alpha-preview-5x.png`. Validation: `pnpm lint`, `pnpm build`, develop-web-game client smoke screenshot, and custom Playwright screenshots under `output/hitbox-debug-visual/` for title activation, stage bullets/walls/enemies, and boss dragon melee.
- Hitbox follow-up: fixed BossScene contact damage to use the player's Arcade body bounds instead of visual `getBounds()`, and removed the separate player/boss Arcade overlap so red debug boxes and actual contact damage use the same custom boss bounds path. Train boss hit bounds were reduced from the full 144x96 visual footprint to a 110x44 lower body box. Player hurtboxes were made taller to include more of the head/face area (bride scaled body 18x39, dragon idle/attack scaled bodies 13.5x36 and 15x36). Bullet hitboxes were reduced (normal player bullet 11.776x11.776 on a 25.6 display, enemy bullet 10.88x10.88 on a 16 display). Validation on clean dev server `http://127.0.0.1:5178/game`: `pnpm lint`, `pnpm build`, custom Playwright confirmed train contact causes no damage with a 3px red-box gap and causes damage with 2px overlap; screenshots are under `output/hitbox-debug-followup/`. The develop-web-game client still captures a black canvas in this Phaser/WebGL setup, so custom Playwright screenshots remain the visual source of truth.
- Hitbox tuning pass: shifted the bride hurtbox right by increasing the hero X offset from 24 to 28, reduced dragon melee collision to a tighter effect-sized green box (normal 51.48x27.28 at base power) and removed BossScene's extra melee reach bonus. Changed attack/debug outlines from yellow to green. Train boss hitbox keeps the reduced left/right bounds but now extends upward more (110x54). Added smaller custom hit bounds for piano and thesis bosses based on their Arcade bodies. VictoryScene now uses the original `victory-bg` asset again and the current `dragon-player` sprite instead of the older `dragon` sprite; the runtime-generated ending background was removed. Validation: `pnpm lint`, `pnpm build`, custom Playwright screenshots under `output/hitbox-debug-tuning/` for bride/bullet, dragon/train melee, piano/thesis bounds, and victory scene. The develop-web-game client was run again under `output/hitbox-debug-tuning-client/`, but still produces a black Phaser/WebGL canvas capture in this environment.
- Final hitbox polish: expanded the lower edge of piano/thesis boss hit bounds while preserving their tighter top/sides (piano 63.4x45.3, thesis 53.1x59.1 in runtime check). Normal dragon scratch hitbox is now shorter to the right and slightly taller/upward compared with the previous pass (base normal 46.2x30.8 with -2px y offset); charged melee remains unchanged. Investigated the gray box in VictoryScene and confirmed it was a code-drawn temporary cage rectangle, not an image asset, so it was removed rather than replaced with generated art. Validation: `pnpm lint`, `pnpm build`, and screenshots under `output/hitbox-debug-final-tuning/` for dragon scratch, piano/thesis bounds, and victory no-cage scene.
- Follow-up investigation: moved dragon normal scratch generation from sprite-center anchoring to updated Arcade body anchoring (`body.right + 10`) and cleared pending/debug melee strikes before inserting the new strike, preventing stale overlap/position carryover. Runtime comparison shows identical normal scratch bounds in Stage 1, Stage 2, and BossScene: x 98.25, y 524.28, width 40.92, height 33.44 from the same player body. Piano/thesis boss hit bounds now use visual bounds with side/top insets but extend much lower over the body (piano 64.8x58.8, thesis 71.2x75.2). Validation: `pnpm lint`, `pnpm build`, custom Playwright screenshots under `output/hitbox-debug-investigation/`; develop-web-game client was rerun under `output/hitbox-debug-investigation-client/` and still captures a black WebGL canvas in this environment.
- Projectile resource pass: generated a new side-view bouquet candidate with `imagegen`, removed the chroma-key background, and added it as `src/game/assets/sprites/charged_bullet-v3.png`. `BootScene` now loads the v3 bouquet, and the excessive charged-bullet glow was removed so the bouquet silhouette stays readable while rotating. Bride normal flower shots are visually smaller (20.48px display) while their Arcade hitbox is larger relative to the flower (15.56px body) by using source-frame sizing for player projectile bodies. Validation: `pnpm lint`, `pnpm build`, custom Playwright screenshots and metrics under `output/bouquet-projectile-tuning/`. The develop-web-game client was also rerun under `output/bouquet-projectile-client/`, but it still captures a black WebGL canvas in this environment.
- Bouquet roundness revision: generated two 2x2 `imagegen` candidate sheets for compact side/3-quarter Korean bridal bouquet projectiles, split eight candidates, and selected `b4` because it kept a short side handle while reading as a near-square flower mass. Added `src/game/assets/sprites/charged_bullet-v4.png` (56x56) and updated `BootScene` to load it. Runtime metrics show the charged projectile now displays at 45.92x45.92 with a 26.63x26.63 Arcade body, avoiding the previous long silhouette. Validation: `pnpm lint`, `pnpm build`, custom Playwright screenshot/metrics under `output/bouquet-projectile-round/`; develop-web-game client rerun under `output/bouquet-projectile-round-client/` still captures a black WebGL canvas in this environment.
- Thesis enemy alpha / boss HP polish: created `src/game/assets/sprites/enemies-v3.png` by preserving the existing enemy sheet and only correcting the alpha of the `thesis-paper` row, which had overly transparent pixels from chroma-key removal. `BootScene` now loads `enemies-v3.png`. Boss defeat now updates the boss HP HUD to zero before destroying the boss, and `HUD.showBossHP` clamps the filled segment count to avoid stale/invalid bars. Validation: `pnpm lint`, `pnpm build`, custom Playwright screenshots/metrics under `output/thesis-enemy-alpha/` and `output/boss-hp-zero/`; develop-web-game client rerun under `output/thesis-hp-client/` still captures a black WebGL canvas in this environment.
- Thesis enemy alpha-only correction: replaced the previous broad alpha fix with `src/game/assets/sprites/enemies-v4.png`, generated directly from `enemies-v2.png` by changing only the alpha channel of the `thesis-paper` row. All non-thesis rows remain byte-identical, and thesis row RGB values are unchanged; only alpha is softly boosted so the enemy no longer looks washed out while source pixel detail is preserved. `BootScene` now loads `enemies-v4.png`. Validation: `pnpm lint`, `pnpm build`, comparison preview at `output/thesis-enemy-alpha-only/thesis-alpha-only-soft-comparison-8x.png`, and runtime screenshot/metrics at `output/thesis-enemy-alpha-only/stage3-thesis-enemy-v4.png`. develop-web-game client rerun under `output/thesis-alpha-only-client/` still captures a black WebGL canvas in this environment.
- Temporary test rollback: per user request, `BootScene` now loads `src/game/assets/sprites/enemies-v3.png` again so the broader thesis-paper alpha fix can be tested in-game against the softer v4 alpha-only variant. `enemies-v4.png` remains in the workspace for comparison but is not currently referenced. Validation: `pnpm lint` and `pnpm build` pass, with the build output confirming `enemies-v3` is bundled.
- Thesis-paper redraw pass: generated two text-prompt-only candidate strips for a compact opaque thesis manuscript enemy, selected the second candidate, chroma-keyed it, downscaled it into a 4-frame 32x32 row, and created `src/game/assets/sprites/enemies-v5.png` by replacing only the final `thesis-paper` row of `enemies-v2.png`. Rows 0-4 are pixel-identical to `enemies-v2.png`. `BootScene` now loads `enemies-v5.png`. Validation: `pnpm lint`, `pnpm build`, candidate/row previews under `output/imagegen/thesis-enemy-v5/`, runtime screenshot/metrics under `output/thesis-enemy-v5/`. develop-web-game client rerun under `output/thesis-enemy-v5-client/` still captures a black WebGL canvas in this environment.
- Thesis enemy bottom cleanup and dragon claw color pass: created `src/game/assets/sprites/enemies-v6.png` from `enemies-v5.png` by changing only the generated `thesis-paper` row, removing the semi-transparent bottom/floor residue and making the lower paper stack opaque; rows 0-4 remain unchanged. `BootScene` now loads `enemies-v6.png`. Dragon melee claw effects now use power-level color progression: blue, bright green, orange, red, purple for levels 0-4, applied to normal and charged claw slash graphics. Validation: `pnpm lint`, `pnpm build`, thesis comparison preview at `output/thesis-enemy-v5/thesis-row-v5-v6-bottom-cleanup-12x.png`, runtime screenshots/metrics under `output/thesis-claw-polish/`. develop-web-game client rerun under `output/thesis-claw-polish-client/` still captures a black WebGL canvas in this environment.
- Final session polish: enemies and all bosses now show a short hit flash on damage by tint-filling and flickering alpha without adding invincibility. Player attack input now fires normal attacks on press, starts charging while held, and fires charged attacks only on release after the charge threshold. Boss HP UI moved from the top text line to a semi-transparent bar at y=316, just above the item upper bound y=350, with Pokemon-style colors: green above 50%, yellow at 50%-20%, red at 20% or below. Item pickup feedback now shows floating `Power Up`, `HP Up`, `Shield`, or score text, and overflow heart/powerup pickups grant +500 score. Stage configs now include `textTheme`, with stage 2 using a light-theme text palette/stroke for better readability. Validation: `pnpm lint`, `pnpm build`, custom Playwright screenshots and metrics under `output/final-session-polish/`; develop-web-game client rerun under `output/final-session-polish-client/` still captures a black WebGL canvas in this environment.
- Gameplay tuning pass: rebalanced normal stages so stage 1 book/phone are slower and lighter on bullets, camera beams fire less often and only after the camera is on-screen, stage 3 now has 21 spawns with more caps/thesis papers, thesis-paper uses a 5-shot semicircle, and cap is now a slow-entry homing kamikaze. Reworked train rush into right-retreat -> warning box -> fast left rush -> right re-entry, removed rail-spark drops, added train radial burst, replaced piano pedal-thump with grand-recital rapid random notes, and added thesis homing destructible bullets plus doctor-defense safe-zone attack. Validation: `pnpm lint`, `pnpm build`, develop-web-game smoke run under `output/gameplay-tuning-smoke/` (still black canvas in that client), and custom Playwright runtime metrics/screenshots under `output/gameplay-tuning-runtime-final/`; metrics confirmed stage 3 spawn count 21, train rush warning/recovery, piano recital notes, thesis destructible homing bullets, and doctor-defense damage/recovery with no console errors.
- Gameplay tuning follow-up: softened train radial burst by staggering the same 7 shots over time, made train rush warning longer and clearer with 3 full blinks, slowed rush traversal, and disabled train hit/contact bounds plus HP damage during retreat/warning preparation. Camera now can fire 2 beams, but beam targeting locks the camera's Arcade body and position until the beam resolves. Validation: `pnpm lint`, `pnpm build`, and Playwright metrics/screenshots under `output/gameplay-tuning-followup/` and `output/gameplay-tuning-followup-camera-lock-2/`; camera lock metrics show x/y fixed with 0 velocity and 2 shots fired, train warning metrics show HP unchanged and empty hit bounds during warning, with no console errors.
- Safari/WebKit and power balance follow-up: replaced camera beam lock's `body.moves=false` approach with per-frame position/body sync to reduce WebKit freeze risk, moved the camera beam origin slightly left/down to better match the lens, kept camera x/y visually locked through two beams, lowered cap spawn heights and changed kamikaze trigger to x-distance instead of radial distance. Ranged powerups now keep one full-damage main shot while extra straight/diagonal bullets are smaller and deal 0.25 damage; charged shot and dragon melee scale by +25% per power level instead of the previous large jumps. Validation: `pnpm lint`, `pnpm build`, WebKit stage 2 soak under `output/safari-stage2-freeze-check-2/` reached boss warning with no console errors, and runtime damage/cap metrics are under `output/balance-followup-runtime/`.
- Boss/stage balance follow-up: camera enemies now remain position-locked through the full second beam resolution, stage 1 train radial density was reduced to 3 orb bullets, all pooled bullets now get explicit render depth so boss bullets do not hide behind sprites, and train attacks no longer use the thematically mismatched key/shoe-like projectile. Boss transitions now carry player shield state, scroll position, and dropped items into BossScene without fade-in. GameOver retry/title transitions reset run and score immediately and use a fallback delayed scene start so stale stage/score data cannot survive a missed fade callback. Normal shooter enemies enter firing sooner, thesis-paper mobs fire a softer 3-shot semicircle, thesis boss bullet counts were reduced, and its orbiting homing hazard now uses cap-like destructible projectiles. Validation: `pnpm lint`, `pnpm build`, Chromium runtime metrics under `output/boss-flow-balance-followup/`, `output/followup-critical-check/`, `output/followup-camera-lock-check/`, and `output/followup-train-bullet-check/`; WebKit stage 2 sampling in `output/boss-flow-balance-followup/metrics.json` reported no console errors.
- Deep follow-up tuning: identified the remaining camera movement cause as a phase transition bug: after the final beam was scheduled, an expired `attackTimer` could move the camera into `exit` while `beamLockUntil` was still active. Enemy update now keeps telegraph-beam cameras locked across phases until the beam lock fully expires, and `lockBeamBody` uses Arcade `body.reset` for cleaner position/velocity sync. Stage backgrounds now clamp the parallax tile offset to the 860px source image width instead of wrapping, preventing the stage 1 boss approach seam/cut. Bride powerup extras now progress as forward small 1 -> forward small 2 -> forward small 2 + 45-degree up -> forward small 2 + 45-degree up + 22.5-degree up, with no downward extra shot. Thesis boss cap hazards stay upright during orbit/chase, and doctor-defense now flashes darker unsafe zones even when the player is in the safe shield column. Validation: `pnpm lint`, `pnpm build`, custom Playwright metrics/screenshots under `output/deep-followup-runtime-final/` and `output/defense-visual-final/`; the standard develop-web-game client still captures a black WebGL canvas at `output/web-game/shot-0.png`, so custom Playwright screenshots remain the visual source of truth.
- Clarification follow-up: the disappearing projectile complaint was about the player's flower bullets, not enemy bullets, so the enemy bullet visual scale change was reverted. Runtime investigation under `output/player-flower-disappear-investigation/` showed player flowers were being deactivated by normal boss-hit collision at the train's left hitbox edge, which could read as disappearing just before contact because there was no impact feedback. Train hit bounds now start 8px farther inside the sprite on the left, and BossScene now spawns a short flower impact flash where player bullets hit bosses. Validation: `pnpm lint`, `pnpm build`, and `output/player-flower-impact-fix/` metrics/screenshots confirmed impact visuals appear at the deactivation point.
- Player bullet consistency follow-up: investigated the remaining intermittent invisible flower issue as a same-frame lifecycle problem. If a player bullet spawned close enough to a boss/enemy hitbox, it could overlap and be deactivated for damage before render/debug overlay had a frame to draw it, so damage appeared without a visible bullet or hitbox. Added a 50ms minimum visible window for player bullets in `BulletPool`; player bullets remain active/visible/debug-drawable during that window and cannot damage bosses, enemies, or destructible boss hazards until it expires. Runtime stress test under `output/player-bullet-min-visible-check/` placed the player next to the train: at 34ms bullets were active/visible and overlapping but `canDamage=false` with no HP loss; at 50ms damage applied. A hitbox-debug screenshot under `output/player-bullet-debug-visible/` confirms the pre-damage visible/debug state. Camera beam origin was also moved from the camera's left edge to the sprite center (`CAMERA_LENS_OFFSET_X = 0`), with the adjusted screenshot under `output/camera-lens-adjusted/`. Validation: `pnpm lint`, `pnpm build`, custom Playwright checks, and standard develop-web-game client rerun (still black WebGL capture at `output/web-game/shot-0.png`).
- Pattern/asset follow-up: used built-in imagegen to create a pixel-art reference sheet for collectible items and hostile bullets, saved at `output/imagegen/item-bullet-reference/generated-reference.png`. The generated reference included a checkerboard background, so deterministic project sprites were produced as `src/game/assets/sprites/item-pickups-v2.png` (8-frame 32px pickup animation sheet) and `src/game/assets/sprites/enemy_bullets-v3.png` (16-frame 16px hostile bullet sheet), then wired through BootScene/Item/BulletPool. Piano boss patterns were softened: key-wave 4->3, note-fan 4->3, crescendo-drop 8->4, grand-recital 30->18, with note bullets scaled to 1.32x. Camera enemies now fire one beam. Thesis boss cap hazards orbit, steer briefly for ~520ms, then continue on their current trajectory instead of endlessly correcting toward the player. Stage spawn Y values now get deterministic bounded jitter for more vertical variety. Validation: `pnpm lint`, `pnpm build`, runtime metrics/screenshots under `output/pattern-asset-followup/`; standard develop-web-game client still produces a black WebGL screenshot at `output/web-game/shot-0.png`.
- Asset clarification follow-up: replaced the low-quality procedural pickup sheet with `src/game/assets/sprites/item-pickups-v3.png`, extracted from the generated reference so the heart/coin/flower/shield pickups read closer to the preferred concept art. Replaced `enemy_bullets-v3` with `src/game/assets/sprites/enemy_bullets-v5.png`: only the general hostile rows were redrawn into classic round/rugby shooting-game bullets, while the special paper/note/key/steam/cargo/beam rows were preserved from the prior special sheet. `BootScene` now loads item v3 and enemy bullet v5. Validation: `pnpm lint`, `pnpm build`, custom Playwright screenshot/metrics under `output/asset-quality-fix/` confirmed item texture 256x32, enemy bullet texture 32x128, visible item animations, general bullet frames at normal scale, and note bullets still using the larger 1.32x special scale. Standard develop-web-game client rerun under `output/asset-quality-fix-client/` still captures a black WebGL canvas in this environment.
- Defense/HUD/item follow-up: used built-in imagegen to create a pixel-art defense shield and dragon-claw pickup reference, saved at `output/imagegen/defense-shield-claw/generated-reference.png`, then extracted transparent project assets `src/game/assets/sprites/doctor-defense-shield-v1.png` and `src/game/assets/sprites/item-pickups-v4.png`. The thesis boss defense pattern now uses the shield sprite instead of a triangle. Heart pickup frames were inset to avoid right-edge clipping, the HUD now reuses the item heart sprite for HP, and power state appears as item icons below HP with one base icon shown at power 0. Dragon mode displays claw pickup/HUD icons for powerups while bride mode keeps the flower. All pickups now use the same pulse effect instead of rotating powerups. Rugby-shaped hostile bullets now spin in flight. Bride small-shot power angles now progress as 0, 22.5, 45, and 67.5 degrees. Validation: `pnpm lint`, `pnpm build`, custom Playwright screenshots/metrics under `output/defense-item-hud-followup/` confirmed the shield sprite, dragon claw frame 8 animation, HUD icon counts, player bullet angles `[0, 0, 22.5, 45, 67.5]`, and rugby bullet angular velocity 360 with no console errors. Standard develop-web-game client rerun under `output/defense-item-hud-followup-client/` still captures a black WebGL canvas in this environment.
- Asset correction follow-up: regenerated the defense shield and dragon-claw reference with built-in imagegen for a clearer top-facing shield and a single claw/talon item, saved at `output/imagegen/defense-shield-claw-v2/generated-reference.png`. Extracted `src/game/assets/sprites/doctor-defense-shield-v2.png` as a wide 112x72 upward-facing shield and `src/game/assets/sprites/item-pickups-v5.png` with extra gutters for heart/coin frames plus a claw-only dragon powerup. `BootScene` now loads shield v2 and item v5. Dragon powerup pickups render slightly larger, and dragon HUD power icons use a larger display size than flower icons so the single claw remains readable. Fixed thesis boss start-position discontinuity by switching horizontal float offset from `cos` to `sin`, resetting the body at the entry tween endpoint, and syncing the Arcade body during tween updates; runtime sampling showed one `boss-thesis` object throughout entry and no 52px snap after entering. Validation: `pnpm lint`, `pnpm build`, custom screenshots/metrics under `output/item-shield-claw-v2-followup/` confirmed heart/coin gutters, claw-only dragon powerup, top-facing defense shield texture 112x72, single visible thesis boss object during entry, and no console errors. Standard develop-web-game client rerun under `output/item-shield-claw-v2-followup-client/` still captures a black WebGL canvas in this environment.
- Thesis defense/heart balance follow-up: raised bride small extra flower bullets from 0.25x to 0.5x damage while leaving charged-shot scaling unchanged. Used built-in imagegen to create a new red heart and Korean black thesis-book shield reference at `output/imagegen/thesis-defense-heart-v3/generated-reference.png`, then extracted `src/game/assets/sprites/item-pickups-v6.png` and `src/game/assets/sprites/doctor-defense-shield-v3.png`. The item sheet keeps coin/flower/star/claw frames from v5, replaces heart frames with a cleaner centered generated heart, and `BootScene` now loads v6 plus thesis shield v3. Defense shield display is now a 128x82 top-facing black thesis book. Doctor-defense unsafe overlays now use near-black 0.97/0.95 alpha, hold for 700ms, fade over 500ms, and boss recovery is delayed to 1250ms so the blackout reads clearly. Validation: `pnpm lint`, `pnpm build`, custom screenshots/metrics under `output/thesis-defense-heart-v3-followup/` confirmed small bullet damage `[1, 0.5, 0.5, 0.5, 0.5]`, item texture 320x32, defense shield texture 128x82, extended blackout alpha 1 during capture, and no console errors. Standard develop-web-game client rerun under `output/thesis-defense-heart-v3-followup-client/` still captures a black WebGL canvas in this environment.
- Thesis defense/items follow-up: used built-in imagegen to redraw a closed black thesis book shield plus coin/flower/shield pickup candidates, saved at `output/imagegen/thesis-defense-items-v4/generated-reference.png`. Extracted `src/game/assets/sprites/doctor-defense-shield-v4.png` as a closed, unopened thesis-book defense marker and `src/game/assets/sprites/item-pickups-v7.png`, keeping heart/claw from v6 and replacing coin/flower/shield pickups with regenerated versions that have more bottom margin. Coin now carries a visible won-style mark. `BootScene` now loads item v7 and thesis shield v4. Doctor-defense unsafe overlay color was changed back to the red `0x5a0615` family, with high alpha but shorter timing than the previous pass: 260ms hold, 300ms fade, boss recovery at 760ms. Validation: `pnpm lint`, `pnpm build`, custom screenshots/metrics under `output/thesis-defense-items-v4-followup/` confirmed item texture 320x32, closed thesis shield texture 128x82, red overlay fill color `5899797`, and no console errors. Standard develop-web-game client rerun under `output/thesis-defense-items-v4-followup-client/` still captures a black WebGL canvas in this environment.
- Doctor-defense cooldown follow-up: shortened the red blackout timing slightly again to 180ms hold plus 260ms fade, with boss recovery at 620ms after the impact flash starts. Added a thesis boss `DEFENSE_MIN_COOLDOWN` of 9000ms; `doctor-defense` is filtered out of both signature/random pattern selection while on cooldown, falling back to non-defense patterns. Validation: `pnpm lint`, `pnpm build`, custom Playwright metrics under `output/defense-cooldown-shorter/` confirmed 20 cooldown-window pattern selections contained no `doctor-defense`, red overlay fill color remained `5899797`, overlays were gone after the shorter fade, and no page errors occurred. Standard develop-web-game client rerun under `output/defense-cooldown-shorter-client/` still captures a black WebGL canvas in this environment.
- Doctor-defense cooldown value follow-up: changed `DEFENSE_MIN_COOLDOWN` from 9000ms to 7000ms while keeping the cooldown measured from defense pattern start time. Validation: `pnpm lint`, `pnpm build`, and runtime check under `output/defense-cooldown-7000/` confirmed `canUsePattern('doctor-defense')` is false at 6999ms, true at 7000ms, and pattern selection falls back to `book-burst` while still on cooldown. Dev server was restarted at `http://127.0.0.1:5174/game` for this check.
- Piano boss cooldown follow-up: increased piano boss attack intervals from 1320/1080/900ms to 1600/1700/1820ms across phases and added per-pattern recovery offsets in `PianoBoss` (`key-wave`/`note-fan` 360ms, `crescendo-drop` 620ms, `grand-recital` handled by its existing recital state). This makes the next pattern timer start with additional spacing after the current pattern begins, reducing overlap from lingering note/key bullets. Validation: `pnpm lint`, `pnpm build`, and custom Playwright metrics under `output/piano-pattern-cooldown/` showed phase-3 pattern start deltas of 2454ms and 2238ms, with no page errors. Standard develop-web-game client rerun under `output/piano-pattern-cooldown-client/` still captures a black WebGL canvas in this environment.
- Background detail pass: used built-in imagegen to create longer 1290x700 stage 1/2 backgrounds, a bright KAIST campus stage 3 background with pond/carillon/CT building cues, and a separate 430x700 thesis-defense classroom boss background. `BootScene` now loads `stage1-long-distance-bg-v3.png`, `stage2-wedding-prep-bg-v2.png`, `stage3-kaist-campus-bg.png`, and `stage3-defense-classroom-bg.png`; `StageConfig` supports `bossBgKey`, and `BossScene` resets carried scroll when the boss background differs from the stage background. Validation: `pnpm build`, standard develop-web-game client under `output/background-detail-pass-client/`, and custom Playwright screenshots/metrics under `output/background-detail-pass/`; metrics confirmed stage 1/2/3 end parallax offsets stay below the 860px max, and stage 3 boss uses the classroom background with `scrollX: 0`.
- Carillon visual follow-up: regenerated `stage1-long-distance-bg-v3.png` to remove the KAIST-like carillon/bell tower from the Seoul railway/Namsan stage, leaving only plausible station/catenary/city scenery. Regenerated `stage3-kaist-campus-bg.png` so the KAIST carillon pillars read as clear red/green/blue zigzag columns at desktop and mobile scale. Validation: `pnpm build` and Playwright captures/metrics under `output/background-followup-carillon/`; stage 1 end has no freestanding bell/carillon structure, stage 3 mid shows the zigzag pillar profile, and no page errors occurred.
- Carillon direction/detail follow-up: regenerated only `stage3-kaist-campus-bg.png` to refine the KAIST carillon detail: side pillars now mirror inward toward the bell instead of all sharing one zigzag direction, the lower pillar sections begin straighter before the zigzag pattern, and the upper structure reads more like it branches outward from the bell area. Validation: `pnpm build` and Playwright desktop/mobile captures under `output/background-carillon-direction-pass/`; no page errors occurred.
- Stage 3 boss classroom reference redraw: converted the user-provided HEIC lecture-room reference to PNG for inspection, then used built-in imagegen to redraw `stage3-defense-classroom-bg.png` as an empty frontal projection-screen classroom with red acoustic ceiling panels, black beam grid, warm spotlights, dark side walls, and no people/text. Validation: `pnpm build`, standard develop-web-game smoke under `output/classroom-reference-redraw-client/`, and Playwright desktop/mobile boss-scene captures under `output/classroom-reference-redraw/`; metrics confirmed the boss scene uses the non-scrolling `stage3-boss-bg` with no page errors.
- Stage 3 boss classroom furniture/floor/projector pass: regenerated `stage3-defense-classroom-bg.png` to replace side classroom desks with one large central seminar table and chairs on both sides, then updated the procedural `ground-stage3-boss` texture so the runtime lower band reads as indoor lecture-room flooring instead of brick/grass. Regenerated once more so the ceiling projector shows its rear panel/ports/vents from the audience side, correctly facing the screen rather than showing a lens toward the viewer. Validation: `pnpm build` and Playwright desktop/mobile captures under `output/classroom-projector-back-pass/`; no page errors occurred.
- Pause menu implementation pass: added a shared Phaser-drawn `PauseMenu`, moved HUD score to the top center, added a right-side menu button, added transient input reset for mobile controls, and wired pause/restart/exit lifecycle into StageScene and BossScene. `계속하기` resumes, `다시하기` resets score/run state and starts StageScene stage 0 immediately, and `종료하기` resets score/run state and returns to TitleScene. Validation: `pnpm lint`, `pnpm build`, develop-web-game client smoke run, and custom Playwright checks under `output/pause-menu-validation/` covering Stage/Boss pause/resume, Z/X/Enter/Esc keyboard controls, restart/exit transitions, mobile menu-button tap, and menu screenshots; no console/page errors.
- Pause menu button polish: replaced the rectangular HUD menu button with a small circular translucent badge and three gold pixel-style lines. Validation: `pnpm lint`, `pnpm build`, and Playwright click/screenshot check under `output/pause-menu-button-polish/`; the button still opens the paused menu and pauses physics/time.
- Ending credits pass: converted the 14 source photos from `엔딩 크레딧 이미지/` into browser-safe JPEGs under `public/images/game/ending-credits/`, added `EndingCreditsScene`, and inserted it between the final story sequence and the existing score/celebration overlay. Credits show each photo as a pixel/mosaic frame, transition into the real photo, support any-key fast-forward, Space-hold skip, and mobile fast-forward/skip buttons. Validation: `pnpm lint`, `pnpm build`, standard develop-web-game smoke run under `output/ending-credits-client/`, and custom Playwright screenshots/checks under `output/ending-credits-visual/`; all 14 ending-credit textures loaded, fast-forward advanced the slide index, Space hold reached the score overlay, and desktop/mobile checks reported no page errors.
- Hero sprite follow-up: kept the new white blouse/light jeans/white keytar heroine sheet at `src/game/assets/sprites/hero-v15-keytar.png`, restored bride visual scale to `0.36`, kept linear texture filtering to reduce small-scale shimmer, and adjusted the run animation from the uneven generated run range to a balanced selected loop `[4, 9, 11, 14, 11, 9]` at 9fps so both footfalls occupy equal time and the crouched `15` frame no longer causes a stumble. Validation: `pnpm lint`, `pnpm build`, custom Playwright runtime montage at `output/playwright/hero-keytar-run-balanced-montage.png` with no console/page errors. Standard develop-web-game client still captures a black WebGL canvas under `output/hero-keytar-balanced-client/`, so custom Playwright screenshots remain the visual source of truth.
- Dragon size restore: decoupled playable dragon visual scale from `HERO_VISUAL_SCALE` and restored it to `0.27`, bringing the dragon runtime frame height from about 35px back to about 70px while the new heroine remains about 81px. Runtime comparison is at `output/playwright/player-size-bride-dragon-comparison.png`; `pnpm lint` and `pnpm build` pass.
- Dragon sprite style redraw: used built-in imagegen to create a new playable dragon-boy sheet matching the heroine's softer anime sprite style. Final project asset is `src/game/assets/sprites/dragon-player-v12.png`, packed into the existing 384x260 x 24-frame contract. The selected version has a pastel pink open shirt over a white T-shirt, consistent round glasses, bangs covering the forehead, plain yellow branched East Asian dragon antlers with no green/blue horn ornaments, a blue tail, and no separate slash effect in attack frames. `BootScene` now loads v12 and applies linear filtering to `dragon-player`; dragon visual scale remains `0.27`. Validation: custom Playwright runtime screenshots `output/playwright/dragon-v12-idle.png`, `output/playwright/dragon-v12-idle-crop-4x.png`, and `output/playwright/dragon-v12-run-montage.png` show the in-game result with no console/page errors. `pnpm lint` and `pnpm build` pass. Standard develop-web-game client still captures black WebGL output at `output/dragon-v12-client/shot-0.png`, so custom Playwright screenshots remain the visual source of truth.
- Dragon sprite polish follow-up: replaced v12 with `src/game/assets/sprites/dragon-player-v13.png`. The v13 source was prompted for thicker simplified outlines, fewer tiny details, bangs covering the forehead more clearly, and plain yellow branched East Asian antlers. A separate one-hand attack strip was generated and packed into frames 18/19 so the basic attack no longer uses a long two-hand reach. `BootScene` now loads v13; dragon attack animation uses `[18, 19, 18]` at 18fps, and `Player` uses a shorter 150ms melee attack animation lock. Validation: custom Playwright screenshots `output/playwright/dragon-v13-idle-crop-4x.png`, `output/playwright/dragon-v13-run-montage.png`, and `output/playwright/dragon-v13-attack-montage.png` show the updated idle/run/attack with no console/page errors. `pnpm lint` and `pnpm build` pass. Standard develop-web-game client still captures black WebGL output at `output/dragon-v13-client/shot-0.png`.
- Dragon sprite scale/filter follow-up: created `src/game/assets/sprites/dragon-player-v14.png` by high-quality LANCZOS downsampling the v13 sheet from 384x260 frames to 288x195 frames with a light unsharp pass, then restored the same final in-game display size by changing dragon visual scale to `0.36`. This avoids relying on WebGL to aggressively shrink the original 384px-wide frames at runtime. Phaser rendering is now non-pixel-art with antialiasing and linear texture filters for hero/dragon. The dragon run animation now uses the steadier selected loop `[13, 14, 12, 10, 8, 9, 10, 12]` at 9fps instead of every generated run frame, removing the most bouncy/airborne poses from the loop. Victory/GameOver dragon scaling was updated to match the new source scale. Validation: custom Playwright screenshots under `output/playwright/dragon-v14-*.png` showed the smoothed in-game dragon with no console/page errors, while runtime metrics confirmed the final dragon display size remained about 103.68x70.2. `pnpm lint` and `pnpm build` pass; the standard develop-web-game client still captures a black WebGL canvas in this environment.
- Player hurtbox head coverage follow-up: raised both playable character hurtboxes so the face is included while keeping the lower edge nearly unchanged. The heroine hitbox is now 54x170 at offset 52,36. The dragon base/frame hitboxes are now 22-24px wide and 75-76px high with top offsets around 15-17 in logical source coordinates, preserving narrow body/face coverage without adding the horns, hair, attack hand, or tail. Runtime debug metrics under `output/hitbox-head-tuning/metrics.json` show both characters' red hurtbox top sits about 12.96px below the visual sprite top and covers the face region; screenshots are `output/hitbox-head-tuning/bride-hitbox-head.png` and `output/hitbox-head-tuning/dragon-hitbox-head.png`. `pnpm lint` and `pnpm build` pass. The standard develop-web-game client still captures black WebGL output at `output/hitbox-head-tuning-client/shot-0.png`.
- Hero hurtbox and stage-2 timer follow-up: lowered the heroine base hurtbox slightly to 54x160 at offset 52,46 so its top sits around the forehead line instead of high over the hair. Added heroine attack-frame hurtboxes for frames 18/19 at 54x136 offset 52,70 so the crouched keytar attack has a shorter hurtbox while keeping the bottom edge stable. Fixed the stage-2 camera enemy crash by making `Enemy.telegraphBeam` capture the current scene timer, skip delayed beam creation if the enemy was destroyed, and clear enemy bullet/player refs on destroy. Runtime validation under `output/hero-hitbox-enemy-timer-fix/` confirmed idle hurtbox top 16.56px below visual top, attack hurtbox top 25.2px below visual top with height 48.96px, and no console errors after destroying a camera enemy before its delayed beam callback. `pnpm lint` and `pnpm build` pass. Standard develop-web-game client rerun under `output/hero-hitbox-enemy-timer-fix-client/` still captures a black WebGL canvas in this environment.
- Hero note bullet pass: used built-in `imagegen` to create a five-frame magical music-note bullet sheet, then regenerated once after feedback so the dotted-eighth/sixteenth pairs use correct beamed notation. Frame 5 still placed the secondary beam on the wrong side, so that one frame was locally redrawn to read as sixteenth note + dotted eighth note: a shared main beam, short secondary beam only on the left sixteenth-note side, and a dot after the right notehead. Final project asset is `src/game/assets/sprites/basic_bullet-v4-notes.png` (5 frames, 40x40 each); preview is `output/imagegen/note-bullets-v2/basic_bullet-v4-notes-preview-corrected.png`. `BootScene` now loads `basic-bullet` as a spritesheet, and `BulletPool` randomly selects one of the five note frames for each normal heroine bullet, clears old tint state, scales notes to about 23.2px display size, and keeps them upright/non-spinning for readability. Heroine normal bullet damage is now 0.6 per note; all normal notes in a volley share the same size and damage. Power levels now fire note volleys at 0, 18.75, 37.5, 56.25, and 75 degrees as levels add bullets. Validation: `pnpm lint` and `pnpm build` pass. Custom Playwright metrics under `output/note-bullets-validation/metrics-upright.json` confirmed five `basic-bullet` frames are loaded, a max-power volley emits five active notes at the requested angles, all with damage 0.6, display size 23.2x23.2, matching hitboxes, and angularVelocity 0. Screenshot: `output/note-bullets-validation/power4-note-bullets-upright.png`. Standard develop-web-game client rerun under `output/note-bullets-client-final/` still captures black WebGL output in this environment.
- Note bullet quality/motion follow-up: discarded the manually redrawn v4 frame as too degraded, generated five additional `imagegen` samples, and selected the best horizontal-beam sample where the dotted-eighth/sixteenth pairs have correct secondary beam placement without needing manual notation redraw. Packed it as `src/game/assets/sprites/basic_bullet-v5-notes.png` with 5 frames at 64x64 for cleaner downscaling; `BootScene` now uses 64px basic bullet frames, and `BulletPool` scales them to 0.36 so the final display size remains about 23.04px. Added visual-only sine wobble for normal heroine note bullets: 12 degrees amplitude, 520ms period, random phase per bullet. The bullet trajectory, velocity, damage, and hitbox do not rotate with the sprite. Validation: `pnpm lint` and `pnpm build` pass. Custom Playwright metrics under `output/note-bullets-v5-validation/metrics.json` confirmed five frames load, max-power volley still fires at 0/18.75/37.5/56.25/75 degrees, all notes have damage 0.6, display size 23.04x23.04, body size about 17.51x17.51, angularVelocity 0, and visual angles change over time with different phases. Screenshot: `output/note-bullets-v5-validation/power4-note-bullets-v5-wobble.png`; preview: `output/imagegen/note-bullets-v5/basic_bullet-v5-notes-preview.png`. Standard develop-web-game client still captures black WebGL output under `output/note-bullets-v5-client/`.
- Charged staccato chord and treble-clef powerup follow-up: discarded the earlier rejected chord sample and regenerated a closer variant based on the user's preferred second sample, keeping staccato dots below the noteheads and making the five stems a consistent visual length. Final charged-shot asset is `src/game/assets/sprites/charged_bullet-v7-staccato-chord.png`, packed as five 96x96 frames in a 480x96 sheet with preview at `output/imagegen/charged-chord-v3/charged_bullet-v7-staccato-chord-preview.png`. `BootScene` now loads `charged-bullet` as a spritesheet, and heroine charged shots select frame 0-4 from the current power level so the projectile grows from a single staccato quarter note to a five-note staccato chord. Charged chord bullets are kept upright with no spin for notation readability; normal note-bullet wobble is unchanged. Also generated a two-frame treble-clef pickup source with built-in `imagegen`, replaced only heroine powerup frames 4-5 in `src/game/assets/sprites/item-pickups-v8.png`, and left heart/coin/star/dragon-claw frames intact. Heroine powerup pickups now render slightly larger, and bride HUD power icons are 20x20 so the tall treble clef remains readable. Validation: `pnpm lint` and `pnpm build` pass. Custom Playwright metrics under `output/powerup-treble-clef-v8-validation/metrics.json` confirmed item sheet 320x32 with frames 4/5, charged texture 480x96 with frames `[0,1,2,3,4]`, power-level-4 charged frame 4, display size 55.68, and no angular velocity. Runtime screenshot: `output/powerup-treble-clef-v8-validation/powerup-treble-clef-and-charged-v7.png`. Standard develop-web-game client still captures a black WebGL output at `output/powerup-treble-clef-v8-client/shot-0.png`.
- Dragon shield oval follow-up: adjusted only the playable dragon shield profile from 122x116 to 92x128 with a -3px y offset so the shared vertical shield image no longer appears circular around the dragon. The shield hit bounds follow the same profile, now measuring 75.44x107.52 at runtime. Validation: `pnpm lint` and `pnpm build` pass; custom Playwright screenshot/metrics are under `output/dragon-shield-oval-check/` and confirmed the active character is `dragon`, the shield display is 92x128, and no console/page errors occurred. Standard develop-web-game client rerun under `output/dragon-shield-oval-check-client/` still captures a black WebGL canvas in this environment.
- Stage order / piano boss tuning: swapped stage 2/3 gameplay order so stage 2 is thesis/defense and stage 3 is wedding prep/piano, reduced thesis-stage caps to 7 across 17 spawns, increased wedding-stage cameras to 7 across 19 spawns with no first-wave cameras, raised piano HP to 70 with earlier/more frequent special phases, added boss-only bouquet/flower projectile asset loading and a piano `bouquet-shot` pattern, and revised story captions to the new thesis-before-wedding flow. Validation: `pnpm lint`, `pnpm build`, standard develop-web-game smoke run under `output/stage-order-piano-client/` (still black WebGL canvas in that client), and custom Playwright metrics/screenshots under `output/stage-order-piano-runtime/`; metrics confirmed stage order, spawn counts, stage 3 piano boss, phase thresholds, bouquet launch/explosion into 12 flowers, shield clearing flower bullets, and no console/page errors.
- Bouquet tuning / stage skip codes: shortened the bouquet projectile post-launch explosion delay from 1150ms to 760ms, reduced flower fragments from 12 to 8, tinted fragments with varied flower colors, and increased their angular velocity to 540. Piano now gets a blinking blue charge shield during bouquet charge and ignores boss damage while the charge shield is active; damage resumes once the bouquet fires. Added title-screen direct stage debug codes `dgist2` and `dgist3`, which reset run/score and jump straight into stages 2 and 3. Validation: `pnpm lint`, `pnpm build`, standard develop-web-game smoke run under `output/bouquet-skip-tuning-client/` (same black WebGL capture limitation), and custom Playwright screenshots/metrics under `output/bouquet-skip-tuning-runtime/`; metrics confirmed direct stage entry, charge immunity with HP unchanged, shield visibility, bouquet fire, 8 tinted fast-spinning flowers, and no console/page errors.
- Bouquet shield/spiral follow-up: enlarged and repositioned the piano bouquet-charge shield to cover the full piano sprite (runtime shield bounds 145x120 over 92.8x92.8 boss visual bounds). Flower fragments now use a per-bullet spiral state rather than straight-line velocity: radius expands outward while angle rotates counterclockwise, with the same varied tints and 540 angular velocity. Validation: `pnpm lint`, `pnpm build`, standard develop-web-game smoke run under `output/bouquet-spiral-shield-client/` (black WebGL capture limitation unchanged), and custom Playwright screenshots/metrics under `output/bouquet-spiral-shield-runtime/`; metrics confirmed shield coverage, 8 flowers, negative angle deltas for counterclockwise spiral motion, and no console/page errors.
- Bouquet speed/cooldown follow-up: slowed bouquet flower spiral radial speed from 0.78x to 0.52x boss bullet speed, increased bouquet charge time from 650ms to 1000ms, and made bouquet cooldown actually apply after the pattern completes by setting `lastAttack` to `now + 2600ms` at pattern recovery. Validation: `pnpm lint`, `pnpm build`, standard develop-web-game smoke under `output/bouquet-slow-cooldown-client/` (black WebGL capture limitation unchanged), and custom Playwright screenshots/metrics under `output/bouquet-slow-cooldown-runtime/`; metrics confirmed no bouquet projectile by 700ms, bouquet fired after the longer charge, 8 slower spiral flowers, ~2.1s remaining extra cooldown after pattern end, and no console/page errors.
- Bouquet retreat follow-up: bouquet-shot now first moves the piano boss to the far right/back position before starting the blue shield charge, so the projectile launches from the rear of the arena rather than from a mid-screen boss position. Validation: `pnpm lint`, `pnpm build`, standard develop-web-game smoke under `output/bouquet-retreat-client/` (black WebGL capture limitation unchanged), and custom Playwright screenshots/metrics under `output/bouquet-retreat-runtime/`; metrics confirmed retreat to x=372, shield/charge only after retreat, bouquet launch, 8 flower fragments, and no console/page errors.
- Bouquet fixed-speed / phase-order follow-up: changed boss flower fragments from polar radius growth to fixed-speed directional movement with a decaying counterclockwise turn, so fragments no longer accelerate as they move outward. Adjusted the bouquet shield into a lower, wider oval with a smaller blink scale range. Piano boss phases now introduce `grand-recital` at 70% HP, keep it through midgame, and delay `bouquet-shot` until the 25% final phase. Validation: `pnpm lint`, `pnpm build`, standard develop-web-game smoke under `output/bouquet-fixed-speed-client-2/` (black WebGL capture limitation unchanged), and custom Playwright screenshots/metrics under `output/bouquet-fixed-speed-runtime-2/`; metrics confirmed phase thresholds, 8 flowers, roughly stable fragment speed, shield coverage, and no console/page errors.
- Bouquet polar-speed follow-up: revised boss flower fragment motion to a polar component model with constant radial speed and constant tangential speed instead of fixed total directional speed. This preserves steady outward expansion from the bouquet origin while preventing the old constant-angular-speed acceleration at larger radii. Validation: `pnpm lint`, `pnpm build`, standard develop-web-game smoke under `output/bouquet-polar-speed-client/` (black WebGL capture limitation unchanged), and custom Playwright screenshots/metrics under `output/bouquet-polar-speed-runtime-3/`; internal metrics confirmed 8 flowers, radial speed fixed at 86.32, tangential speed fixed at 51.79, and no console/page errors.
- Bouquet tangential-speed follow-up: increased boss flower fragment tangential speed ratio from 0.6 to 0.75 while preserving radial speed, making the spiral turn read more strongly without changing outward expansion speed. Validation: `pnpm lint`, `pnpm build`, standard develop-web-game smoke under `output/bouquet-tangent-faster-client/` (black WebGL capture limitation unchanged), and custom Playwright metrics/screenshot under `output/bouquet-tangent-faster-runtime/`; metrics confirmed 8 flowers, radial speed 86.32, tangential speed 64.74, and no console/page errors.
- Bouquet radial-acceleration follow-up: added mild radial acceleration to boss flower fragments (`22px/s^2`) with a 1.22x radial speed cap, while keeping the faster constant tangential speed. Validation: `pnpm lint`, `pnpm build`, standard develop-web-game smoke under `output/bouquet-radial-accel-client/` (black WebGL capture limitation unchanged), and custom Playwright metrics/screenshot under `output/bouquet-radial-accel-runtime/`; metrics confirmed radial speed ramps from 86.32 to capped 105.31, tangential speed stays 64.74, 8 flowers remain active, and no console/page errors.
- Bouquet flower fade follow-up: kept the current polar spiral speed model and added distance-based fade/despawn for boss flower fragments. Flowers fade from radius 150 to 210 and deactivate at the end of that band, preventing late distant fragments from lingering and reading as slowed projectiles. Validation: `pnpm lint`, `pnpm build`, standard develop-web-game smoke under `output/bouquet-flower-fade-client/` (black WebGL capture limitation unchanged), and custom Playwright metrics/screenshot under `output/bouquet-flower-fade-runtime/`; metrics confirmed alpha remains 1 before radius 150, fades to 0.68 at radius 169.1 and 0.19 at radius 198.4, then all 8 flowers deactivate with no console/page errors.
- Bouquet no-retreat/frequency follow-up: removed the pre-charge rightward retreat from piano `bouquet-shot`, so the boss now starts shield charge immediately at its current position. Increased final-phase bouquet frequency by adding a second `bouquet-shot` entry to the pattern list, and reduced bouquet recovery from 2600ms to 1900ms. Validation: `pnpm lint`, `pnpm build`, standard develop-web-game smoke under `output/bouquet-no-retreat-client/` (black WebGL capture limitation unchanged), and custom Playwright metrics/screenshots under `output/bouquet-no-retreat-runtime/`; metrics confirmed immediate charge at unchanged boss x, 2 final-phase bouquet entries, bouquet firing from current position, shortened cooldown, and no console/page errors.
- Hero extra-shot balance follow-up: changed ranged normal fire so the base note always fires, while powerup-added extra notes fire every other normal attack by powerup parity. Odd-numbered extras (1st/3rd) fire on one volley and even-numbered extras (2nd/4th) on the next, so level 2+ avoids attacks with no extra projectile. Validation: `pnpm lint`, `pnpm build`, standard develop-web-game smoke under `output/hero-extra-shot-alternate-client/` (black WebGL capture limitation unchanged), and custom Playwright metrics/screenshot under `output/hero-extra-shot-alternate-runtime/`; metrics confirmed level 0 remains 1 shot, level 1 alternates 2/1 shots, level 2 alternates 18.75/37.5 extras every shot, level 3 alternates 3/2 shots, level 4 alternates 3/3 shots, and no console/page errors.
- Hero extra-shot mod-4 follow-up: reduced ranged powerup extra-shot frequency further by assigning each added note to one slot in a 4-shot cycle instead of a 2-shot parity cycle. The base note still fires every attack; extra notes 1/2/3/4 fire on shots 1/2/3/4 of the cycle respectively. Validation: `pnpm lint`, `pnpm build`, standard develop-web-game smoke under `output/hero-extra-shot-mod4-client/` (black WebGL capture limitation unchanged), and custom Playwright metrics/screenshot under `output/hero-extra-shot-mod4-runtime/`; metrics confirmed level 0 remains 1 shot, level 1 pattern 2/1/1/1, level 2 2/2/1/1, level 3 2/2/2/1, level 4 2/2/2/2, and no console/page errors.
- Hero progressive extra-shot follow-up: changed ranged powerup extra shots so each newly unlocked extra starts on a 4-shot cycle, while previously unlocked extras become one cycle step faster as power level increases. At max power, extra 1 fires every shot, extra 2 every 2 shots, extra 3 every 3 shots, and extra 4 every 4 shots; the base note still fires every attack. Validation: `pnpm lint`, `pnpm build`, standard develop-web-game smoke under `output/hero-extra-shot-progressive-client/` (black WebGL capture limitation unchanged), and custom Playwright metrics/screenshot under `output/hero-extra-shot-progressive-runtime/`; metrics confirmed the 8-shot sequences for levels 0-4 and no console/page errors.
- Hero progressive extra-shot mod-5 follow-up: changed the newly unlocked ranged extra-shot starting cycle from 4 to 5 while preserving the progressive cycle reduction for older extras. At max power, extra 1 fires every 2 shots, extra 2 every 3 shots, extra 3 every 4 shots, and extra 4 every 5 shots; the base note still fires every attack. Validation: `pnpm lint`, `pnpm build`, standard develop-web-game smoke under `output/hero-extra-shot-progressive-mod5-client/` (black WebGL capture limitation unchanged), and custom Playwright metrics/screenshot under `output/hero-extra-shot-progressive-mod5-runtime/`; metrics confirmed 10-shot sequences for levels 0-4 and no console/page errors.
- Bouquet random-right reposition follow-up: restored piano bouquet pre-charge repositioning, but now it moves to a random x in the right half of the arena (`GAME_WIDTH * 0.55` through `GAME_WIDTH - 72`) instead of the far-right edge. Charge shield and bouquet charging begin only after the reposition tween completes. Validation: `pnpm lint`, `pnpm build`, standard develop-web-game smoke under `output/bouquet-random-right-client/` (black WebGL capture limitation unchanged), and custom Playwright metrics/screenshots under `output/bouquet-random-right-runtime-2/`; six isolated attempts landed at x 245-344 within the expected 236.5-358 range, charge/shield/immune all activated, and no console/page errors occurred.
- Difficulty mode pass: added Easy/Hard selection from the title start action plus a `dgist2`/`dgist3` stage-select debug panel with difficulty selection. Added centralized difficulty config/state, Easy HP 5 and no powerup drops on hit, Easy boss item sequence with two extra hearts, Hard-specific stage wave tables, Hard camera shotCount 2, Hard boss HP 1.3x, Hard boss cooldown scaling 0.8x, and Hard piano bouquet double burst with the second spiral rotating in the opposite direction. Validation: `pnpm lint` and `pnpm build` pass. Standard develop-web-game client still captures a black WebGL canvas at `output/difficulty-client/`, so custom Playwright screenshots are under `output/difficulty-runtime/`. Runtime checks confirmed Easy starts at 5/5 HP and drops 0 powerups on hit, Hard stage 3 has 25 spawns with 11 cameras and camera `shotCount: 2`, Hard boss HP is train/thesis/piano 650/910/1300, Easy train boss drop interval reflects six boss drops, and Hard piano bouquet reaches 16 active flower bullets after the second burst.
- Easy follow-up: changed Easy hit handling so the player's current power level is preserved on damage instead of resetting to 0 with no dropped items. Easy stage 1 spawns now carry `shotCountOverride: 0`, making stage 1 mobs move normally but never fire bullets. Validation: `pnpm lint`, `pnpm build`, and a custom Playwright runtime check confirmed Easy damage returns `droppedPowerups: 0` while keeping power level 3, the first Easy stage 1 spawn has `shotCountOverride: 0`, and no enemy bullets are active after the first spawn window.
- Dragon/bouquet balance follow-up: reduced dragon normal melee range/effect from 74 base range with 1.0 visible ratio to 62 base range with 0.9 visible ratio, and reduced charged melee from 96 base range with 0.86 visible ratio to 78 base range with 0.78 visible ratio; per-power growth was also lowered. Hard piano bouquet second burst delay increased from 220ms to 800ms so the second clockwise flower burst arrives after a readable gap. Validation: `pnpm lint` and `pnpm build` pass. Custom Playwright confirmed normal dragon melee runtime bounds are now about 55.8x33.3 and Hard bouquet flower counts progress 8 -> 8 -> 16 across the added delay.
- Dragon scratch size follow-up: reduced dragon scratch effect/collision further after playtest feedback. Normal melee is now 54 base range with 0.78 visible ratio and smaller per-power growth, while charged melee is now 68 base range with 0.68 visible ratio and smaller per-power growth. Validation: `pnpm lint`, `pnpm build`, and custom Playwright confirmed base normal scratch bounds are now about 42.1x25.1.
- Hard boss/minion and shield follow-up: matched the dragon shield profile to the heroine shield exactly (`82x116`, y offset 0, same hit bounds), increased Hard boss HP multiplier to 1.5x, and added Hard-only BossScene minions. Boss fights now periodically spawn at most one minion from the current stage's Hard enemy set; killed minions grant kill score and drop one random item from coin/heart/powerup/star. Player bullet and melee minion collision is resolved before boss damage so minions can be cleared cleanly. Validation: `pnpm lint`, `pnpm build`, and custom Playwright confirmed Hard boss HP is now 600/825/1200 with the current base boss HP values, a Hard stage 3 boss minion spawns, and a random item drop is created.
- Hard hit penalty follow-up: changed Hard damage penalty from dropping/resetting all powerups to reducing current power by exactly one level with no dropped items. Easy still preserves power completely on hit. Validation: `pnpm lint`, `pnpm build`, and a UI-path Playwright check confirmed Hard damage from power level 3 returns `droppedPowerups: 0` and leaves power level 2.
- Final balance follow-up: Hard damage now reduces current power by one level and drops exactly one powerup item when the player had power to lose. Hard stage 3 camera pressure was reduced from all cameras firing twice to manual overrides: 4 of 11 cameras fire twice and the rest fire once. Hard boss minion item drops were nerfed from guaranteed to 50% chance. Validation: `pnpm lint`, `pnpm build`, and custom Playwright confirmed Hard stage 3 camera distribution 4 two-shot / 7 one-shot, Hard damage from power level 3 leaves power level 2 and spawns one powerup drop, and boss minion drop chance gates at 50%.
- Title UI/background polish pass: removed the title story description panel and boxed start/difficulty modal treatment, changed both game modes' start label to `시작하기`, and replaced the title backgrounds with imagegen-generated pixel-fantasy composites matching the current player sprite outfits. After feedback, discarded the floating-island staging and settled on cohesive bright/dark fantasy vistas: `src/game/assets/backgrounds/title-bg-bride-fantasy-pixel-v3.png` and `src/game/assets/backgrounds/title-bg-dragon-fantasy-pixel-v4.png`. The dragon title art was regenerated with a taller heroine-like proportion, relaxed stance, no hand flame, and a blue-dominant night palette. Finally, removed the old title perspective/warble canvas path so the 430x700 title illustrations render directly at 430x700 with no tint, cover crop, or per-frame slice distortion. Validation: `pnpm lint`, `pnpm build`, and Playwright screenshots under `output/title-direct-bg/` confirmed the direct-render title screens and text-only Easy/Hard selection show with no console/page errors.
- Bride title proportion follow-up: runtime checks confirmed the title background is no longer stretched (`source/display 430x700`, `scaleX=1`, `scaleY=1`) and mobile/tablet CSS preserves the 430:700 ratio. The remaining wide feel was in the generated heroine art, so the bright title background was regenerated as `src/game/assets/backgrounds/title-bg-bride-fantasy-pixel-v4.png` with a narrower, taller heroine proportion closer to the dragon title art while preserving the white blouse, jeans, and keytar outfit. `BootScene` now loads v4. Validation: `pnpm lint`, `pnpm build`, and Playwright screenshots under `output/title-bride-v4/` confirmed the new bride/dragon title screens and direct 1:1 background display with no console/page errors.
- Dragon title proportion follow-up: regenerated the dark title background as `src/game/assets/backgrounds/title-bg-dragon-fantasy-pixel-v5.png` so the dragon character matches the accepted bride v4 proportions more closely: slimmer shoulders/torso, longer readable legs, relaxed pose, no hand flame, and a slim trailing tail. `BootScene` now loads v5. Validation: `pnpm lint`, `pnpm build`, and Playwright screenshots under `output/title-dragon-v5/`; runtime metrics confirmed both bride and dragon title backgrounds render at `430x700` with `scaleX=1`, `scaleY=1` and no console/page errors.
- Boss radial spacing balance pass: widened radial/fan bullet gaps for the taller player art. Thesis boss `book-burst` offsets widened from `±0.38` to `±0.48`, and `orbit-spread` now uses explicit offsets `[-0.84, -0.42, 0.42, 0.84]` instead of the tighter `0.33rad` step pattern. Piano boss `note-fan` offsets widened from `±0.52` to `±0.72`, and bouquet flower bursts were reduced from 8 to 6 flowers, increasing circular spacing from 45 degrees to 60 degrees. Validation: `pnpm lint`, `pnpm build`, and custom Playwright checks under `output/boss-radial-spacing/` confirmed thesis orbit angles at `pi±0.42/pi±0.84`, piano note fan emits the widened `±0.72rad` lanes, bouquet bursts spawn 6 flowers, and no console/page errors occurred.
- Powerup drop economy pass: reduced regular stage powerup drops to one per stage for both Easy and Hard, replacing removed powerups with coin/heart/star drops. Boss item sequences were also shifted away from powerups: Easy boss drops now contain one powerup across an 8-item sequence with more hearts/stars/coin, and Hard boss drops now use `heart, powerup, star, coin`. Hard boss minion drops keep the 50% drop chance but use weighted items `coin, coin, heart, heart, star, powerup`, lowering effective powerup drops to about 8.3% per killed minion. Hard boss minion first spawn delay increased from 6200ms to 11000ms so the boss opening is not immediately combined with a minion. Validation: `pnpm lint`, `pnpm build`, and Playwright runtime checks under `output/drop-balance/` confirmed stage drop histograms have one powerup per stage per difficulty, boss sequences match the new tables, and UI-selected Hard boss scenes have no minion during the opening with first minion scheduled around 11s.
- Powerup drop economy correction: adjusted the regular stage powerup count back up to two per stage for both Easy and Hard after feedback that one powerup made misses too punishing. Hard boss minion timing now uses the normal minion interval as the first delay (`HARD_BOSS_MINION_FIRST_DELAY = HARD_BOSS_MINION_INTERVAL = 8200ms`) instead of a longer special delay, meaning the boss fight starts with no minion and the first minion appears only after one normal cooldown cycle. Validation: `pnpm lint`, `pnpm build`, Playwright stage-drop histograms confirmed every stage in both difficulties has exactly two `powerup` drops, and UI-selected Hard boss runtime showed `nextBossMinionSpawnAt = 8200ms`.
- Hard boss minion immediate-spawn bug fix: fixed the real timing bug where `BossScene.create()` scheduled the first Hard minion with `this.time.now` but `updateHardBossMinions(time)` compared against the update callback's `time`, which can be on a different/global timeline after stage-to-boss transitions. BossScene now initializes the Hard minion timer on the first `update(time)` call, setting `nextBossMinionSpawnAt = time + HARD_BOSS_MINION_FIRST_DELAY` and returning without spawning. This keeps the existing 8200ms cadence but prevents immediate minion spawn even if the game clock has already advanced before BossScene starts. Validation: `pnpm lint`, `pnpm build`, and a Playwright repro with Hard selected and several seconds elapsed before direct BossScene start confirmed `hardBossMinionTimerStarted=true`, `nextBossMinionSpawnAt` about 7.8s in the future, and `activeMinionObjects=0` shortly after boss scene entry.
- Final item/score balance pass: reduced star/shield item duration from 5000ms to 4000ms. Added `scoreMultiplier` to difficulty config (`easy=1`, `hard=1.5`) and made ScoreManager dynamically read the current difficulty for score gains from kills, bosses, coins, overflow points, and clear bonuses while leaving hit/death penalties unchanged. Difficulty state is mirrored on `globalThis.__weddingDifficultyId` so dev-server dynamic imports and runtime scene imports cannot diverge. Validation: `pnpm lint`, `pnpm build`, standard develop-web-game smoke under `output/final-item-score-client/` (black WebGL capture limitation unchanged), and custom Playwright checks under `output/final-item-score-runtime/` confirmed Easy score actions produce 8800, Hard produces 13200, selected/global difficulty matches the UI choice, no console errors occur, and `ITEM_CONFIGS.star.value` is 4000.
- Item lifetime / shield warning correction: restored the star shield effect duration to 5000ms because the requested duration adjustment was for dropped items, not the shield buff. Dropped field items now use explicit lifetime constants (`DROPPED_ITEM_VISIBLE_MS = 8000`, `DROPPED_ITEM_FADE_MS = 500`) instead of the previous 10000ms visible delay. Shield warning blink now pauses the soft alpha tween in the last 2s and uses duty-cycle visibility: fast warning is visible for 105ms of each 280ms cycle, critical warning is visible for 42ms of each 170ms cycle, making the hidden time longer than the visible time. Validation: `pnpm lint`, `pnpm build`, standard develop-web-game smoke under `output/item-lifetime-shield-blink-client/` (black WebGL capture limitation unchanged), and custom Playwright checks under `output/item-lifetime-shield-blink-runtime/` confirmed star effect 5000ms, dropped item lifetime 8000ms + 500ms fade, warning/critical hidden samples outnumber visible samples, and no console errors occurred.
- GameOver character/copy pass: reduced the heroine GameOver sprite from the old 1.875 scale to a shared 184px target display height, matching the dragon GameOver display height while preserving each spritesheet's aspect ratio. Dragon GameOver copy now uses the same `GAME OVER` title and `다시 도전하기` retry label as the heroine mode. Validation: `pnpm lint`, `pnpm build`, standard develop-web-game smoke under `output/gameover-size-copy-client/` (black WebGL capture limitation unchanged), and custom Playwright screenshots/metrics under `output/gameover-size-copy/` confirmed both heroine and dragon GameOver sprites display at 184px height, the dragon title/retry labels match, and no console errors occurred.
- Difficulty input repeat fix: TitleScene now consumes repeated keyboard events (`KeyboardEvent.repeat`) while the Easy/Hard difficulty panel or hidden stage-select panel is open, and the title start shortcut ignores repeated Enter/Space/Z/X events. This prevents holding the start key from immediately confirming Easy/Hard as soon as the difficulty text appears; the player must release and press again. Validation: `pnpm lint`, `pnpm build`, standard develop-web-game smoke under `output/difficulty-repeat-input-client/` (black WebGL capture limitation unchanged), and custom Playwright/CDP checks under `output/difficulty-repeat-input/` confirmed `autoRepeat: true` Enter leaves the game in `TitleScene`, while a fresh Enter advances to `StoryScene`, with no console errors.
- Mobile touch-control pass: temporarily disabled mobile ranged autofire behind `MOBILE_RANGED_AUTOFIRE_ENABLED = false` while leaving the existing Player autofire methods recoverable. Reworked `TouchControls` action buttons so each action pointer keeps its originally pressed button active and adds/removes the other action button while the finger drags into/out of that button. Releasing the finger clears both active action contributions; dragging back to the original button releases only the secondary action. Validation: `pnpm lint`, `pnpm build`, standard develop-web-game smoke under `output/mobile-touch-client/` (same black WebGL capture limitation), and custom mobile Playwright/CDP checks under `output/mobile-touch-runtime/` confirmed no idle autofire bullets, jump->attack->back releases only attack before final jump release, attack->jump->back releases only jump before final attack release, releasing over the secondary button clears both action pointers, and no console errors occurred.
- Mobile touch-control v2: swapped the mobile action button order to `ATK | JUMP` so it mirrors PC `X | Z` action placement as requested. Refined action semantics: attack is now latched per finger once the pointer starts in or drags into the attack area, and it releases only when that finger lifts; jump is momentary and fires a fresh jump/down event every time the pointer enters the jump area, then jump/up when it leaves or lifts. Validation: `pnpm lint`, `pnpm build`, standard develop-web-game smoke under `output/mobile-touch-v2-client/` (black WebGL capture limitation unchanged), and custom mobile Playwright/CDP checks under `output/mobile-touch-v2-runtime/` confirmed label positions (`ATK` x=268.75, `JUMP` x=376.25), no idle autofire, attack stays held while dragging into/out of jump, jump re-triggers on each jump-area entry, and both pointers clear on release with no console errors.
